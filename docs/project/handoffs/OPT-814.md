# OPT-814 — authored EB03/EB04/P/PRB02 fidelity

Original base: `15cad98bd139fc0d8cea075c809e4f8a7b45f507` (`main`); current integration base is recorded below. Isolated workspace `/private/tmp/optcg-opt814`, branch `codex/opt-814-card-fidelity`. The implementer never merges PRs or writes Linear; no unmerged sibling code was imported. OPT-821 Life-position implementation is in the base. PR and final validation revision are recorded by the coordinator; this handoff is part of the reviewed head.

## Source and acceptance mapping

Sources: canonical `docs/cards/EB-03.md`, `EB-04.md`, `PRB-02.md`, `UNKNOWN.md`; `docs/FAQs/qa_prb02.md` (004 mandatory activation/zero/once-per-turn, 006 legal alternate rest); Comprehensive Rules v1.2.0 §§1-3-5-1 (zero), 8-3-1/8-3-1-8 (cost/activation timing), 8-3-2 (turn conditions), 8-1-4 (continuous effects), 8-6 (ordering), 3-1-6 (fresh zone identity). No current-source ambiguity was used to invent a ruling.

| Clause | Evaluation / chooser / sequence | Change and observable regression |
| --- | --- | --- |
| EB03-018 opponent-turn effect K.O. protection | Continuous, regardless of active/rested | Remove unprinted active condition; rested Tashigi survives authored X.Drake effect K.O. |
| EB04-003 opponent-turn Navy Leader base7000 | Continuous, regardless of host active/rested | Remove active condition; pipeline recomputes7000 while rested; own-turn/non-Navy remain5000 |
| EB04-035 DON on **your** field returned | Event controller, your turn, Kid Pirates Leader, once per turn | Add SELF event filter; Sheep's Horn cost return adds rested DON; opponent Ulti-Mortar return during your attack does not offer it |
| EB04-038 DON comparison scopes draw and subsequent DON | Block eligibility before draw; controller chooses up-to DON | Move comparison to block; own8 vs opponent5 draws/adds neither;8/9 allow draw then one active DON |
| P-085 owner's Life top/bottom face-up | OnPlay, Supernovas and Life comparison; effect controller chooses target/end | Add TOP_OR_BOTTOM; both ends target owner's Life with stable existing order; zero targets gives no position prompt |
| PRB02-004 mandatory auto, up-to1, once per turn | Opponent attack; controller chooses0/1 | Remove optional/decline flags; quantity prompt directly, both0/1 consume once-per-turn |
| PRB02-006 opponent-turn gate | Replacement match time | Add IS_MY_TURN OPPONENT; Izo on opponent turn can rest another Character instead; opponent Punk Gibson during Zoro's own turn cannot offer replacement |
| PRB02-006 opponent's **Character** source | Causing effect provenance | **Document option used; not implemented.** Existing replacement API only gets effect/battle and causing controller. Authored Sheep's Horn Event incorrectly offers replacement (standalone failing probe). The source-snapshot prerequisite from OPT-812 is now merged, but Character-source propagation/filtering through batch replacement and continuation remains OPT-847. No ignored source filter added. |
| PRB02-009 **this Character**, rested **by opponent effect** | Explicit host subject, transition toRESTED, effect cause relative to host controller | Add SELF subject filter, effect-rest provenance (normal and replacement continuation), narrow host-scoped matcher. Izo rests host → optional trash-self then draw2; sibling/attack/ACTIVE event do not activate. |

PRB02-006's exact ticket permits adding a source card-type filter **“(or document)”**; that alternative is used openly, not presented as full card fidelity. Coordinator preserved this documented alternative and created [OPT-847](https://linear.app/optcg-sim/issue/OPT-847/complete-authored-rest-trigger-host-cause-and-character-source) for the remaining behavior. The actual source restriction remains defective.

## Verification and red evidence

- Final regression file: `workers/game/src/__tests__/opt-814-authored-fidelity.test.ts`. Registered production schemas through `getEffectSchema`; real `runPipeline` actions and `resumePromptLifecycle` complete pending effects/replacements. Active-event guard additionally exercises trigger matching after a real pipeline action.
- Final23 tests restored to original base implementation: **10 failed /13 passed** across all eight corrected cards (`/private/tmp/opt814-baseline-finaltests.log`). Initial red-before-edit capture also in `/private/tmp/opt814-red.log`.
- Corrected implementation initially23 passed, then added batch replacement-decline provenance case: **25 passed**, `/private/tmp/opt814-green.log`.
- Decisive guards mutation: removed host identity, RESTED state, and opponent-effect guard; sibling/own-attack/active-event tests each fail (**3 failed/20 passed**), `/private/tmp/opt814-guard-mutation.log`. Restored tracked code then23green.
- Later full gate exposed three stale tests: two Bonney assertions expected obsolete optional decline, and a synthetic source-zone rest event omitted RESTED. Corrected those tests against FAQ/new transition contract; OPT814+OPT244+m4c focused102passed (`/private/tmp/opt814-followup-green.log`).
- Own-effect provenance test uses registered OP07-036 Dragon Twister; removing only the causing-controller inequality produces1failure, restored (`/private/tmp/opt814-controller-mutation.log`).
- Related rest publication, nested rest ordering, field-card rest, replacement schema suite: **15 passed in4files**, `/private/tmp/opt814-related.log`.
- `pnpm type-check:worker`: passed before final docs-only changes, `/private/tmp/opt814-type.log`.
- `pnpm schema:check`: passed;2472 schemas,3821 authored actions,34 tests; generated production registry rebuilt, `/private/tmp/opt814-schema.log`.
- Full `pnpm verify` first attempt: lint/type/bundle/schema and app2360passed (7 existing DB skips); pipeline47passed/1facet snapshot mismatch because Bonney was the last authored lock_on_decline tag. Updated only that obsolete snapshot entry; rerunning full gate. Full base gate was not run.
- **Final `pnpm verify` passed (exit0)** on `75b5f1e6799f69bef406b3a5a25eb62d203d9b1a`, `/private/tmp/opt814-verify-complete.log`: lint0errors/271warnings; app+worker types; workerbundle; schema34passed; app2360passed/7pre-existing DBskips; pipeline48passed; worker2511passed/5existing skips; production Nextbuild. Workercoverage83.45%statements/75.13%branches/89.5%functions/87.3%lines. No new skipped tests. Network was granted for build Fonts; no DATABASE_URL disposable test service was supplied.
- Final handoff-only update links OPT847 and records this result; full gate carries forward across that documentation-only delta. Required external CI and independent/coordinator reviews remain coordinator-owned.
- Additional replacement-continuation provenance mutation: removing effect cause from the resumed batch publisher fails Issho→Zoro decline→Mr.3 trash/draw regression (1failed; `/private/tmp/opt814-resume-mutation.log`); restored publisher.

Source-kind gap probe is retained as `workers/game/src/__tests__/opt-814-source-kind-gap.test.ts.txt`, outside passing test discovery. Copy to the same path without `.txt`, run `pnpm --filter optcg-game exec vitest run src/__tests__/opt-814-source-kind-gap.test.ts --maxWorkers=1`, remove only the copied file. It produces **1 failure** because Sheep's Horn leaves an OPTIONAL_EFFECT replacement prompt instead of resting Zoro (`/private/tmp/opt814-gap.log`). No skipped or expected-failure test is concealed in the passing gate.

## Shared consumer inventory and bounded contract

A recursive Object.entries walk over **every nested value** of `getAllAuthoredSchemas()` found10 `CHARACTER_BECOMES_RESTED` triggers: OP07-031, OP10-036, OP14-021/027/028/032/035/070/119, PRB02-009. Read each clause in its schema/card text; OP07-031 and OP10-036 watch **a Character**, while other host-worded consumers require separate encoding assessment.

Host matching is explicit `EventFilter.target: SELF`; broad watcher default is retained. New effect-cause matching is intentionally supported only for explicitly host-scoped rest triggers. Unscoped cause-filter consumers OP07-031/OP10-036 (BY_YOUR_EFFECT), OP14-070 (BY_OPPONENT_EFFECT) retain their prior unsupported cause behavior. Enabling Buffalo without its Character-source restriction would introduce incorrect effects; no card-ID exception is used. Pipeline regression checks unchanged Perona/Buffalo behavior. `docs/game-engine/02-TRIGGERS.md` now documents this support boundary and removes the incorrect universal host-scope claim.

Effect rest payload adds optional `cause: EFFECT` and `causingController`; declaration and rest-cost publishers do not falsely label themselves effect rests. The replacement continuation rest publisher carries the same provenance. No source snapshots or OPT-812 prohibition changes duplicated.

## Follow-ups

- PRB02-006 Character-source fidelity: standalone failing source-kind probe above; source and departed-source identity must survive prompt continuations. Related OP14-070 Buffalo also needs host and Character-source fidelity. Tracked by [OPT-847](https://linear.app/optcg-sim/issue/OPT-847/complete-authored-rest-trigger-host-cause-and-character-source).
- Other unscoped rest-cause watchers and host-worded rest consumers require their own authored execution work; inventory above is scope accounting, not a claim of correctness.
- Preserve approximations explicitly named in OPT-814: EB03-053 Nami, EB03-057 Yamato, EB04-054 Kuma still encode printed “up to1” Life clauses as fixed `amount:1`. No change in this PR.
- Full local verification passed; coordinator/independent review, current main integration, required CI/repository approval discovery remain readiness gates. Stop at merge-ready; never merge.


## Independent review correction — durable rest provenance

P1 at https://github.com/corycunanan/optcg-sim/pull/658#discussion_r3976064514 was confirmed: shared event types and emitters included cause/controller but `session/persisted-game-state.ts` still rejected those keys in its strict CARD_STATE_CHANGED payload. This could prevent session restoration after hibernation. Added only optional literal `cause: EFFECT` and optional PlayerIndex0|1; strict unknown-key rejection is retained.

Actual registered Izo→Mr.3 and Issho→Zoro replacement-decline→Mr.3 sequences were saved through SessionRepository, serialized into in-memory storage, and loaded through a fresh SessionRepository. Both failed before the fix with “unknown event variant” (`/private/tmp/opt814-persistence-red.log`,2failures). Both now load the pending trigger and complete trash-self/draw2 once, then save/load the completed result. Legacy attack-rest payload without provenance remains loadable; invalid cause, invalid controller, and unknown payload fields still reject. The expanded authored suite passes33tests; worker typecheck passes (`/private/tmp/opt814-persistence-green.log`, `/private/tmp/opt814-persistence-type.log`). Renewed `pnpm verify` passed with exit0 at `11cb7122864f05a3701b826c6d77e807c0d426af` (`/private/tmp/opt814-persistence-verify.log`): schema34, app2360, pipeline48, worker2519 passed; existing skips unchanged; build/type/lint/bundle green. This final handoff-only receipt carries that gate across its documentation delta; earlier readiness is superseded and independent delta review remains required.

Runtime-reader inventory: `shared/game-types.ts` defines the payload; `session/persisted-game-state.ts:hasValidEventPayload` is the strict validator consumed by `session/persistence.ts` for persisted events/continuations; `engine/visibility.ts` marks rest events public; `session/history.ts` compacts them; `src/lib/sandbox/apply-event.ts:changeFieldCardState` reads subject/state fields and ignores additional provenance. Repository searches across shared, session, engine visibility, hooks, and app lib found no second strict CARD_STATE_CHANGED payload schema. No validator was changed to accept arbitrary keys.

External review thread has not been replied to or resolved by the implementation agent. This section and the PR body record the disposition for the coordinator's independent delta review.


## Integration after OPT-811/812/808 delivery (2026-09-10)

Normally merged main `da56870a4e62e6a40394a0cd190a4cedbbdd9b7c` into reviewed head `a5fb177e3230eb7efe1d4de3b5a634dfb155e9a7`. Only the generated registry conflicted; regenerated and compared every entry to both sides. All2472 schemas are the exact union of eight OPT-814 cards and20 incoming OP07/08/13–16 corrections, with no overlapping card edit or lost entry (`/private/tmp/opt814-integration-registry-union.json`). Main's generated target-instruction snapshot was retained and independently executed; the earlier Bonney facet snapshot remains intact.

Three overlapping handwritten files merged cleanly. SET_REST now retains OPT-812's causing-controller/live-source/snapshot prohibition check and OPT-814's effect-cause publication. Replacement feasibility retains OPT-812's source-aware checks while resumed rest publication retains OPT-814's cause/controller fields. effect-types.ts retains the rest SELF subject, TRASH_COUNT filter, and source-snapshot contracts. Inspected both parent-relative diffs; no manual behavior edit was needed. Other incoming files match main byte-for-byte. The strict persisted CARD_STATE_CHANGED P1 correction is unchanged and its actual SessionRepository tests run with the integrated source/provenance code.

Combined authored814/808/811/812, target snapshot, rest/blocker, nested rest, field-card rest, OPT820 and OPT821 verification:15 files,229 tests passed (`/private/tmp/opt814-main808-focused.log`). The standalone PRB02-006 Sheep's Horn source-kind probe was copied temporarily from the committed `.test.ts.txt`, rerun and removed in a finally block: exactly one expected real failure remains (replacement incorrectly offered), `/private/tmp/opt814-main808-gap.log`. This confirms integration did not implement or conceal the documented OPT847 limitation. The merged OPT812 snapshot support is now available for that separately scoped follow-up; no new source-kind filter was encoded here.

Full `pnpm verify` is renewed against the committed integration/handoff tree. Its exact final head/result is recorded in PR658 body and coordinator receipt after completion. Prior independent review and mutations apply to unchanged OPT814 implementation; coordinator integration assessment and fresh remote gates remain required. No PR merge or review-thread communication was performed by this implementer.
