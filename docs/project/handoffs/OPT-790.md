# OPT-790 — Roger DON!! Phase

Implementation delivered for independent review; not a merge-readiness receipt.

Read `workers/game/src/engine/phases.ts` and `workers/game/src/__tests__/opt-790-don-phase.test.ts` first. The DON!! step evaluates Leader rule-modification eligibility against the state before placement, then gives only the eligible newly placed DON!! to the Leader. Existing cost-area and attached DON!! retain their identities. Placement, actual giving, and MAIN transition events remain ordered.

## Rules and scope

Sources: local supported Comprehensive Rules v1.2.0 §§6-2-3–4, 6-4-1–3; `docs/cards/OP-13.md` (OP13-003); `docs/game-engine/RULES-TO-ENGINE-MAP.md` §6-4; [official OP13 FAQ, p1](https://en.onepiece-cardgame.com/pdf/qa_op13.pdf?20251024=), verified 2026-09-13.

| Clause | Evaluation/target | Observable result | Evidence |
| --- | --- | --- | --- |
| Any DON!! on your field | Before placement; active Leader's controller | Empty fields cannot self-qualify | Both seats, both first-player settings, later empty fields |
| One placed DON!! given to Leader | Only newly placed identities; no cost/choice | One attached, remaining new DON!! active in cost area | 0/1/2 deck cards, existing cost and attached DON!! |
| Standard DON!! phase | First player first turn one, otherwise two; deck availability cap | Exact placement and give events | Authored-schema public pipeline tests |

Recursive traversal of `getAllAuthoredSchemas()` found exactly one `DON_PHASE_BEHAVIOR` consumer: OP13-003, count 1, GIVEN_TO_LEADER. No authored consumers use GIVEN_TO_CHARACTER or PLACED_RESTED; those unimplemented union alternatives remain outside this ticket. Schema text and Roger's existing power penalty are unchanged. No UI, protocol, migrations, or manual deployment changes.

## Validation

Baseline at a31499ba6f8d0787443501ff9c022e962e0bf01f: existing pipeline suite 39/39 passed. New regression before implementation: 4 failed, 14 passed, with all qualifying nonempty-deck cases failing on missing attachments. Fixed initial regression plus pipeline: 57/57 passed. Added attached-only Leader/Character eligibility and ordered-event cases: 61/61 passed.

`pnpm verify` passed lint, app/worker types, bundle, schema gate (34 tests), app suite (2361 passed, 7 skipped), pipeline suite (48 passed), and worker coverage (2768 passed, 5 skipped). Build initially failed Google Fonts DNS resolution in the sandbox; network-enabled `pnpm build` retry passed. Database suites were skipped by the standard runner without TEST_DATABASE_URL. Final test additions are test-only; targeted tests, ESLint, and worker types rerun after them. A temporary mutant evaluating the condition after placement failed 8/22 cases; restored production source passed all 61 focused tests. See PR body for commit pointer.

## Handoff

No ticket dependency introduced. Sibling OPT-788 and OPT-791 surfaces are untouched. Coordinator owns integration with latest main, independent review, readiness, and any authorized merge. No Linear writes or merges performed by implementer. Locate the commit by its `(OPT-790)` subject and the PR linked from the run ledger.
