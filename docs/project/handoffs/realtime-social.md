---
linear-project: Realtime Social
linear-project-url: https://linear.app/optcg-sim/project/realtime-social-bf3d7344f863
last-updated: 2026-05-03 · OPT-360 in review; OPT-359 in review
---

# Realtime Social — Handoff Doc

Replace four cross-player polling loops with a single per-user `UserChannel` Durable Object. Real presence ships as a side-effect. Project just created from OPT-88; all 11 tickets in Backlog, none started.

---

## Action Plan

Tickets in execution order. Ordering criteria: dependencies → estimate → priority → risk.

| Order | Ticket | Title | Estimate | Depends on | Status | PR | Notes |
|-------|--------|-------|----------|------------|--------|----|-------|
| 1 | [OPT-351](https://linear.app/optcg-sim/issue/OPT-351) | Extract `useAuthedWebSocket` shared hook | 3 | — | Done | [#205](https://github.com/corycunanan/optcg-sim/pull/205) + [#206](https://github.com/corycunanan/optcg-sim/pull/206) merged | Pure refactor. Follow-up #206 landed a generation counter fix Codex flagged after #205 merged — guards two pending-`connect()` races. |
| 2 | [OPT-352](https://linear.app/optcg-sim/issue/OPT-352) | `UserChannel` DO scaffold + `/api/realtime/token` | 5 | — | Done | [#207](https://github.com/corycunanan/optcg-sim/pull/207) merged | Server-only foundation. Adds `UserChannel` DO + 3 worker routes + Next.js token mint endpoint. Dev game flow unchanged. |
| 3 | [OPT-353](https://linear.app/optcg-sim/issue/OPT-353) | `notifyUser` helper + `useUserChannel` + app-shell wiring | 3 | OPT-351 ✓, OPT-352 ✓ | Done | [#208](https://github.com/corycunanan/optcg-sim/pull/208) merged | Last foundation ticket. `notifyUser` server helper + `useUserChannel` hook + `<UserChannelProvider>` mounted in root layout. No events sent yet — OPT-354 lands the first one. |
| 4 | [OPT-354](https://linear.app/optcg-sim/issue/OPT-354) | Chat push (P1) — delete 5s chat poll | 3 | OPT-353 ✓ | Done | [#209](https://github.com/corycunanan/optcg-sim/pull/209) merged | First polling loop deleted; +60s backstop. Establishes the per-poll-replacement pattern for T5–T7. |
| 5 | [OPT-355](https://linear.app/optcg-sim/issue/OPT-355) | Friends push (P2) — delete 30s sidebar poll | 3 | OPT-353 ✓ | Done | [#210](https://github.com/corycunanan/optcg-sim/pull/210) merged | Four event types; +60s backstop. Pure reducer mirrors `applyMessageEvent`. |
| 6 | [OPT-356](https://linear.app/optcg-sim/issue/OPT-356) | Lobby push (P3a) — delete 1.5s lobby poll | 3 | OPT-353 ✓ | Done | [#211](https://github.com/corycunanan/optcg-sim/pull/211) merged | Single event type; +30s backstop (lower than P1/P2 — terminal states bound drift). State builder extracted to `src/lib/lobbies/build-state.ts`. |
| 7 | [OPT-357](https://linear.app/optcg-sim/issue/OPT-357) | Game status push (P3b) — delete 2s game status poll | 2 | OPT-353 ✓ | Done | [#212](https://github.com/corycunanan/optcg-sim/pull/212) merged | No backstop (one-shot terminal state). `notifyGame` helper + `after()` fanout from all three `GameSession.status` mutators; consumer recomputes `winnerPerspective`/`canFallbackConcede` per-recipient. |
| 8 | [OPT-358](https://linear.app/optcg-sim/issue/OPT-358) | Real presence (P4) — `User.lastSeen` + DO bookkeeping + UI | 5 | OPT-353 ✓ | Done | [#213](https://github.com/corycunanan/optcg-sim/pull/213) merged | First schema change. `presence:friend_online` / `presence:friend_offline` events; multi-tab safety via 5s offline-debounce timer cancellation. DO-to-DO fanout via `env.USER_CHANNEL.idFromName(friendId).fetch()` — no Next.js round-trip per recipient. |
| 9 | [OPT-359](https://linear.app/optcg-sim/issue/OPT-359) | Stretch: typing indicators + read receipts (P5a/b) | 4 | OPT-354 ✓ | In Review | [#214](https://github.com/corycunanan/optcg-sim/pull/214) | First client→server event vocabulary. `RealtimeClientEvent` discriminated union; DO inbound validation + 1.5s throttle; `Message.readAt` schema change; `POST /api/messages/[userId]/read` replaces implicit GET-marks-read. |
| 10 | [OPT-360](https://linear.app/optcg-sim/issue/OPT-360) | Stretch: lobby invite notifications (P5c) | 3 | OPT-356 ✓ | In Review | — | First non-poll-replacement feature. New `LobbyInvite` schema + 4 routes + sender popover + recipient toast. |
| 11 | [OPT-361](https://linear.app/optcg-sim/issue/OPT-361) | Cleanup: drop the 60s backstop polls | 1 | OPT-354–358 | Backlog | — | Soak gate: 7 days, <1% fanout failure rate |

**Status values:** use Linear status names verbatim (`Backlog`, `Todo`, `In Progress`, `In Review`, `Done`, `Canceled`). Don't invent.

**Next up:** OPT-361 (cleanup of 60s backstop polls). Gated on a 7-day soak window after OPT-358 merge (2026-05-02) — earliest unblock is 2026-05-09. OPT-359 + OPT-360 PRs need to merge first to clear the in-review queue, but neither blocks OPT-361 directly.

**Total:** ~35 points.

---

## Handoffs

Append new entries at the bottom. Each entry is written *by* the agent who just finished a ticket, *for* the agent who picks up the next ticket.

<!--
Copy this block when writing a new handoff:

### OPT-XXX → OPT-YYY
**From:** session on YYYY-MM-DD · **Commit:** `<short-sha>` · **PR:** #NN

- **Primer:** <1 sentence — what changed at the system level>
- **Read first:** `path/to/file.ts`, `path/to/other.ts`
- **Gotchas / do NOT touch:** <what to leave alone and why, OR "none">
- **Unresolved:** <follow-ups, open questions, deferred work, tracking IDs — OR "none">
- **Why this matters for OPT-YYY:** <1–2 sentences tying the above to the next ticket's surface>

-->

### Project setup → OPT-351
**From:** session on 2026-05-01 · **Commit:** *(uncommitted; project + tickets land in OPT-351's PR)* · **PR:** —

- **Primer:** OPT-88 was an umbrella analysis ticket. Promoted to a Linear project (Realtime Social) with 11 tickets covering foundation refactor → poll-by-poll migration → presence → stretch features → cleanup. SCOPE doc at `docs/project/REALTIME-SOCIAL-SCOPE.md` is the architectural source of truth.
- **Read first:** `src/hooks/use-game-ws.ts` (the hook OPT-351 generalizes — supersede-safe `onclose` at `:158-181` is the OPT-350 invariant; preserve verbatim). Then `docs/project/REALTIME-SOCIAL-SCOPE.md` §Architecture sketch.
- **Gotchas / do NOT touch:** Do **not** rename `GAME_WORKER_SECRET`, do **not** change `useGameWs`'s public surface, do **not** introduce a different reconnect heuristic. The token-on-every-reconnect contract is non-negotiable per OPT-350's incident notes.
- **Unresolved:** Whether the worker secret should be renamed `WORKER_SECRET` (cosmetic; not in scope for any current ticket). Whether the `verifyGameToken` should be split into two helpers or a single parameterized one (decided in OPT-352 — author's call).
- **Why this matters for OPT-351:** OPT-351 is a pure refactor with a strict no-behavior-change constraint. The supersede-safe close logic is the highest-value invariant in the existing hook; the new generic hook **must** preserve it line-for-line. Test coverage of that invariant should be the first thing OPT-351 lands.

### OPT-351 → OPT-352
**From:** session on 2026-05-01 · **Commit:** `314087a` · **PR:** [#205](https://github.com/corycunanan/optcg-sim/pull/205)

- **Primer:** WebSocket transport extracted from `useGameWs` into `src/hooks/use-authed-websocket.ts`. The hook is a thin React adapter over a pure factory `createAuthedWebSocketController` (mirrors `createScenarioRunner` so lifecycle is testable without React). `useGameWs` is now ~120 lines of game-vocabulary message routing; its public return shape is unchanged. OPT-352 does **not** consume the new hook (server-only ticket) but lands on the same project foundation.
- **Read first:** `src/hooks/use-authed-websocket.ts` (the new shared hook OPT-353 will consume — don't fork it). `workers/game/src/GameSession.ts` + `workers/game/src/util/auth.ts` + `workers/game/src/util/token-replay.ts` (patterns OPT-352's `UserChannel` DO should mirror — hibernation accept, JTI consumption, Bearer-secret notify endpoint).
- **Gotchas / do NOT touch:** Do **not** edit `useAuthedWebSocket` to add game-specific or user-channel-specific concerns — it's transport only. Reuse the same `JWT_SECRET` / `WORKER_SECRET` envs that `GameSession` uses; do not introduce parallel secrets. The OPT-350 supersede invariant is now in `shouldHandleClose` (pure helper) — preserve verbatim if anything in that area is touched downstream.
- **Unresolved:** Whether `verifyGameToken` should be split into two helpers vs. parameterized with an audience claim — author's call in OPT-352 (SCOPE doc §Locked design decisions calls for shared `JWT_SECRET`, no `gameId` claim in user tokens). One unchecked item from this PR's test plan: manual 2P-game smoke + Solitaire Strict Mode WS-count check (OPT-350 regression) — should be run before merge.
- **Why this matters for OPT-352:** OPT-352 is independent on the wire (server-only DO scaffold + `/api/realtime/token` endpoint) but consumes the same auth conventions. The new hook is irrelevant *for OPT-352 directly* but is the future client of OPT-352's DO via OPT-353 — keep the WS handshake protocol exactly compatible (`?token=<HS256>` query param, `verifyGameToken`-shape verification on the worker side). The token URL appending (`?token=` separator handling) is now centralized in `createAuthedWebSocketController` — OPT-352's wrangler routes should accept that exact URL shape.

### OPT-352 → OPT-353
**From:** session on 2026-05-02 · **Commit:** `4fdcacc` · **PR:** [#207](https://github.com/corycunanan/optcg-sim/pull/207)

- **Primer:** `UserChannel` Durable Object lives at `workers/game/src/UserChannel.ts`, keyed by `env.USER_CHANNEL.idFromName(userId)`. Worker exposes `/user/:userId/{ws,notify,health}`. `verifyUserToken` is parallel to `verifyGameToken` (same shared secret, no `gameId` claim — and **rejects** tokens that *do* carry `gameId`, to prevent game-token misrouting). `consumeGameTokenJti` was renamed to `consumeTokenJti`; both DOs use it against their own per-DO storage.
- **Read first:** `workers/game/src/UserChannel.ts`, `workers/game/src/index.ts` (router shape — both `/game/*` and `/user/*` patterns), `src/app/api/realtime/token/route.ts` (the mint endpoint OPT-353's hook should call once on mount, then re-mint on every reconnect using `useAuthedWebSocket`'s existing `tokenFetcher` contract).
- **Gotchas / do NOT touch:** Wire OPT-353's `useUserChannel` on top of `useAuthedWebSocket` (OPT-351's hook) — do **not** fork transport. The token URL contract is `?token=<jwt>` (query param, single-use JTI). The DO's `notify` endpoint accepts an opaque body — OPT-353's `notifyUser(userId, payload)` server helper should `JSON.stringify(payload)` and POST it; the DO fans the raw bytes out unchanged. No event vocabulary is defined here; OPT-353 lands `notifyUser`, OPT-354 sends the first event. Idle reap is 10 minutes of zero sockets — exposed via the `alarm()` handler, not a timer.
- **Unresolved:** None. The "split vs parameterize verifier" question from OPT-351's handoff is resolved — went with parallel `verifyUserToken` for clarity (gameId-bearing tokens are rejected, not silently accepted). The cosmetic `GAME_WORKER_SECRET` → `WORKER_SECRET` rename is still deferred; not required for any current ticket.
- **Why this matters for OPT-353:** OPT-353 is the first ticket to **consume** OPT-352's surface. The hook should mint via `POST /api/realtime/token`, dial `wss://<worker>/user/<userId>/ws?token=<jwt>` via `useAuthedWebSocket`, and ignore inbound messages until OPT-354 defines the first one. The server-side `notifyUser` helper goes against `POST /user/<userId>/notify` with `Authorization: Bearer ${process.env.GAME_WORKER_SECRET}` — same pattern Next.js already uses for `POST /game/.../notify-end`. OPT-358's presence work depends on `getConnectionCount()`, which is already exposed as a public method on the DO.

### OPT-353 → OPT-354
**From:** session on 2026-05-02 · **Commit:** `623b340` · **PR:** [#208](https://github.com/corycunanan/optcg-sim/pull/208)

- **Primer:** Foundation phase complete. The bridge between server routes and the per-user `UserChannel` DO is wired end-to-end but inert — `RealtimeServerEvent` is `never` and no events flow until OPT-354. New code: `notifyUser()` (server helper, 2s timeout, best-effort, structured warn logs); `useUserChannel()` (client hook composing `useAuthedWebSocket`, mints via `POST /api/realtime/token` on every reconnect, dispatches by `type` through a typed `Map<eventType, Set<handler>>`); `<UserChannelProvider>` mounted in the root layout to enforce single-socket-per-tab.
- **Read first:** `src/types/realtime.ts` (the empty discriminated union OPT-354 extends — add `{ type: "message:new"; ...payload }`), `src/lib/realtime/fan-out.ts` (call `notifyUser(recipientId, { type: "message:new", ... })` from the chat POST route), `src/lib/realtime/event-dispatcher.ts` (the dispatcher; note the `[T] extends [never]` non-distributive guards — once OPT-354 adds the first variant, the conditional flips and `EventType`/`EventOf<T>` narrow to the real union), `src/components/realtime/user-channel-provider.tsx` (subscribers call `useUserChannelEvents().subscribe("message:new", handler)`).
- **Gotchas / do NOT touch:** The provider is mounted **once** in `src/app/layout.tsx` inside `<SessionProvider>`. Do not mount a second one and do not call `useUserChannel()` directly outside the provider — every direct call opens its own socket. The `subscribe` function identity is stable across re-renders (memoized on the dispatcher), so it's safe in `useEffect` deps. Don't add retry logic to `notifyUser` — the 60s reconciliation backstop polls (still in place in T4–T7) are the safety net until OPT-361 removes them. The hook intentionally surfaces `disconnected` (not `connecting`) when signed-out so the future status indicator doesn't flicker on logout.
- **Unresolved:** No connection-status UI surfaced — deferred to T8 (presence) and T11 (cleanup) per the ticket. Manual E2E (real Cloudflare worker, "Offline" throttle, multi-tab) **not run** in this session — verified locally via unit tests + type-check only. Would be worth a 5-minute browser pass before merging the first migration ticket lands events on top.
- **Why this matters for OPT-354:** OPT-354 is the first ticket to **send** an event through the channel. Pattern: extend the union in `src/types/realtime.ts` with `{ type: "message:new"; messageId: string; conversationId: string; from: string; body: string; sentAt: string }`; from the chat POST route, after the DB insert, call `notifyUser(recipientUserId, { type: "message:new", ... })`; in the chat widget, replace the 5s poll with `useUserChannelEvents().subscribe("message:new", ...)` to refetch / append. Keep the 60s reconciliation poll in place (per project plan) — OPT-361 deletes all backstops together after a 7-day soak.

### OPT-354 → OPT-355
**From:** session on 2026-05-02 · **Commit:** `047e9c9` · **PR:** [#209](https://github.com/corycunanan/optcg-sim/pull/209)

- **Primer:** First polling loop replaced. `RealtimeServerEvent` now carries `{ type: "message:new"; message: SerializedMessage }`. POST `/api/messages/[userId]` fires-and-forgets `notifyUser(toUserId, …)` after the create, using a new `serializeMessageForEvent()` helper to convert Prisma's `Date` → ISO string. `ChatWidget` subscribes via `useUserChannelEvents().subscribe("message:new", …)`; the 5s `setInterval` is now a 60s reconciliation backstop. Dedup happens both in the push handler (`applyMessageEvent`) and the backstop poll — the sender's own optimistic POST-then-setMessages races against the server fanout, and the backstop's `?after=…` query can re-fetch a pushed-then-persisted message during the same window. Establishes the pattern for OPT-355–358: extend union → fan out from POST route → subscribe in component → 60s backstop.
- **Read first:** `src/types/realtime.ts` (extend the union — add e.g. `{ type: "friend-request:new"; ... }`), `src/lib/realtime/serialize-message.ts` (clone this pattern for friend-request payloads — the goal is one tiny serializer per event type, taking a Prisma row + returning the wire shape), `src/lib/realtime/fan-out.ts` (the fanout helper signature is now `RealtimeServerEvent`-typed end-to-end — adding a variant immediately propagates through `notifyUser`, the dispatcher's `EventType`/`EventOf<T>`, and the provider's `subscribe`), `src/components/social/chat-widget.tsx` (the canonical subscribe + backstop pattern — mirror it for friends sidebar), `src/components/social/apply-message-event.ts` (pure dedup/filter helper — the equivalent for friends will be a 4-event reducer over the `User`+`FriendRequest` join the sidebar already renders).
- **Gotchas / do NOT touch:** Don't extract `notifyUser` into a per-route wrapper — the helper's contract is "fire and forget with structured warn"; routes call it directly with `void notifyUser(...)`. The dispatcher's `as unknown as Handler<EventType>` casts at lines 51 / 55 of `event-dispatcher.ts` are intentional — handlers stored in a Set keyed by event type, and the cast is the bookkeeping price. Don't replace `setInterval(60_000)` with `setTimeout`-loop or `requestIdleCallback` cleverness — the simple interval is what keeps OPT-361's cleanup mechanical (one-line removal per widget). Keep the visibility gate on the backstop (`if (minimized) return`) — it mirrors the existing one and prevents thundering-herd polls when many widgets exist on screen.
- **Unresolved:** Two-browser manual smoke (A sends → B sees in <1s) **not run** in this session — verified via unit tests + type-check + lint. Same gap as OPT-353's deferred E2E; should be a single 5-minute pass before merging the OPT-355–358 batch in aggregate. The "stale modal state" feedback memory doesn't apply here (no modal), but watch for it once OPT-359 adds typing indicators.
- **Why this matters for OPT-355:** OPT-355 swaps the friends-sidebar 30s poll for **four** event types (`friend-request:new`, `friend-request:accepted`, `friend-request:rejected`, `friend-request:removed`). The shape of the work is identical — extend the union with four variants, serialize from each `POST /api/friends/*` and `DELETE /api/friends/:id` route, subscribe in `social-sidebar.tsx` (or wherever the 30s poll lives — confirm with `grep -n "30_000\\|setInterval(30" src/components/social`). The dedup story is different: the sidebar renders a **list of relationships**, not a chat log, so the reducer is "upsert by relationship-id" plus "remove on rejected/removed" rather than "append unique by id." Worth designing the reducer pure (mirror `applyMessageEvent`) before wiring it into React — the test surface is bigger than chat's.

### OPT-355 → OPT-356
**From:** session on 2026-05-02 · **Commit:** `c833368` · **PR:** [#210](https://github.com/corycunanan/optcg-sim/pull/210)

- **Primer:** Second polling loop replaced. `RealtimeServerEvent` now carries four `friend:*` variants (final names: `friend:request_received`, `friend:request_accepted`, `friend:request_declined`, `friend:removed` — the OPT-354 handoff sketched `friend-request:*` but the Linear ticket spec wins). The four friend API routes fire-and-forget `notifyUser(otherParty, …)` after their DB mutation; the 30s sidebar poll becomes a 60s backstop. Pattern is mechanical now: tiny serializer in `src/lib/realtime/serialize-<feature>.ts`, pure reducer in `src/components/<feature>/apply-<feature>-event.ts`, subscribe inside the component, keep the 60s backstop.
- **Read first:** `src/lib/realtime/serialize-friend.ts` + `src/components/social/apply-friend-event.ts` (current canonical templates — clone shape, change vocabulary). `src/components/lobbies/use-lobby-room.ts` around the 1.5s poll (the OPT-88 breadcrumb at `:123` per the action plan; confirm with `grep -n "setInterval\\|1_500\\|1500" src/components/lobbies src/hooks` since the file may have moved). `src/types/realtime.ts` (extend with `lobby:*` variants — typical shape is one event per state transition, e.g. `lobby:player_joined`, `lobby:player_left`, `lobby:ready_state_changed`, `lobby:started`). `src/app/api/lobbies/**` for the fanout sites.
- **Gotchas / do NOT touch:** Lobby is a **multi-user broadcast**, not a per-recipient push like chat / friends. `notifyUser` targets a single user — to broadcast to N lobby members you call it N times. That's fine for typical lobby sizes (1–4) but call it after the DB transaction commits, not inside it (otherwise a fanout failure can rollback the join). Don't introduce a new "broadcast to room" helper — until presence (OPT-358) needs it, the per-user fan-out is exactly what `UserChannel` provides. The accepter's identity in friend-events comes from `session.user` (`src/app/api/friends/requests/[id]/route.ts`); for lobby fanouts the same shape works — read `session.user.{id,username,name,image}` straight off `requireAuth()`'s return. Keep `friend:request_declined` as a no-op subscribe in the sidebar even when re-touched — it's deliberate (no outgoing-requests UI today; the variant exists for the union to be closed).
- **Unresolved:** Two-browser manual E2E for OPT-355's four flows **not run** in this session (same pattern as OPT-353/354 — verified via unit + type + lint). Worth a single 5-minute multi-browser pass before merging OPT-355 / OPT-356 / OPT-357 in aggregate. None of the OPT-354 review items (CodeRabbit / Codex) are open against the friends path.
- **Why this matters for OPT-356:** OPT-356 deletes the most aggressive remaining poll (1.5s lobby room poll). The mechanics are the same as friends, but the *shape of the receiver's state update* is closest to `apply-friend-event.ts` — small list, mutate-in-place, dedup by id. Use the friends serializer/reducer as the template (chat's append-only history is a poorer fit). After OPT-356, only OPT-357 (game status, 2s, terminal) and OPT-358 (presence, schema change) remain before OPT-361 cleanup.

### OPT-356 → OPT-357
**From:** session on 2026-05-02 · **Commit:** `dfb1b98` · **PR:** [#211](https://github.com/corycunanan/optcg-sim/pull/211)

- **Primer:** Third polling loop replaced. `RealtimeServerEvent` now carries `{ type: "lobby:state_changed"; lobby: LobbyRoomState }` — a **whole-state-snapshot** event, not a per-mutation diff (single event type covers join / mode / deck / ready / start / close / eject). Single source of truth for the wire shape lives in `src/lib/lobbies/state.ts` (types, pure helpers) + `src/lib/lobbies/build-state.ts` (DB → state assembler) — both `GET /api/lobbies/[id]` and the new `notifyLobby()` produce the exact same shape so the room hook never sees drift. New helper `notifyLobby(lobby, { actorUserId })` calls `notifyUser` for each member except the actor; solitaire (host = guest) collapses to one call via `Set` dedup. CLOSED (host-close) and EVICTED (forced PVP→Solitaire eject) are single-recipient `notifyUser` calls with a `{ ...state, status }` override, not via `notifyLobby`. `useLobbyRoom`'s 1.5s `visibilitychange`-gated poll becomes a 30s reconciliation backstop — lower frequency than chat/friends because the room flow has explicit terminal states (Start → game route, Close → home).
- **Read first:** `src/lib/lobbies/state.ts` + `src/lib/lobbies/build-state.ts` (the reusable shape — clone for OPT-357 if game status needs a similar builder, but more likely you'll just serialize a small `{ status, mode, gameId, canFallbackConcede }` payload from `useRemoteGameStatus`). `src/lib/realtime/fanout-lobby.ts` (one-liner per-member fanout — OPT-357's game status is single-recipient most of the time; only fan to the *opponent* on terminal state, like chat). `src/app/api/lobbies/[id]/route.ts` PATCH/DELETE (the eject and CLOSED-override patterns — useful if game status ever needs viewer-specific status overrides, e.g. CONCEDED-to-self vs WON-to-other). `src/hooks/use-lobby-room.ts` (the canonical subscribe + `if (event.lobby.id !== lobbyId) return` id-filter pattern — OPT-357's hook will need the same gameId filter). For the hook test, see `src/hooks/use-lobby-room.test.tsx` (React mock + captured subscribe handler — port directly).
- **Gotchas / do NOT touch:** **Always call `after()` from `next/server`** — bare `void notifyUser(...)` gets cancelled when Vercel Fluid Compute tears the runtime down, and the cb's outer awaits (build state → fan out) make the timing window large. The route tests use a tracked `afterCalls.pending` queue + `flushAfter()` because the cb's chain has multiple awaits — port that pattern (don't try `void cb()` like the friends/chat tests). Fanouts run in `after()` so they stay outside the response-timing budget — keep them there. The `LobbyRoomState` `status` enum has `EVICTED` and `CLOSED` as **virtual** statuses (DB only knows WAITING/READY/IN_GAME/CLOSED — see `prisma/schema.prisma:243`); routes synthesize them at the response/event boundary, never write them to DB. The new `src/lib/lobbies/` directory co-exists with the existing `src/lib/lobbies.ts` file (file wins for `@/lib/lobbies` imports; the dir is reached via explicit subpaths like `@/lib/lobbies/state`) — don't merge them without checking the `normalizeLobbyCode` callsite still resolves. DELETE's guest-fanout branch is dormant today (DELETE only handles WAITING lobbies, which by definition have no guest); kept as forward-compat — don't delete it just because it's untriggered.
- **Unresolved:** Two-browser manual E2E **not run** this session (mode flip / Start / forced eject / host close — same gap as OPT-353/354/355). Worth a single 5-minute pass before merging OPT-355–357 in aggregate. CodeRabbit/Codex haven't reviewed yet — none of OPT-354/355's review items touched the lobby path. `src/lib/lobbies.ts` (file) ↔ `src/lib/lobbies/` (dir) co-existence is a footgun for future contributors; logged as a cosmetic cleanup, not in scope for any current ticket.
- **Why this matters for OPT-357:** OPT-357 deletes the 2s `useRemoteGameStatus` poll. Unlike P1/P2/P3a, the ticket spec calls for **no backstop** — game status has a one-shot terminal transition (IN_PROGRESS → COMPLETED/ABANDONED) and the existing WS already carries gameplay events, so a missed status push is recoverable on next page load. The structural shape mirrors OPT-356 closely (whole-state snapshot, single event type, id-filtered subscribe) — the main divergence is the *trigger surface*: status changes are written from the **game worker** (Durable Object), not from a Next.js route. That means OPT-357 needs a worker-side `notify-user` call (the worker already has the secret to hit `/user/:id/notify`; see `workers/game/src/util/auth.ts`'s outbound auth pattern). Don't extend `LobbyRoomState`-style server-builder for this — game status is small enough to inline. After OPT-357, only OPT-358 (presence, schema change) remains before OPT-361 cleanup.

### OPT-357 → OPT-358
**From:** session on 2026-05-02 · **Commit:** `34acf62` · **PR:** [#212](https://github.com/corycunanan/optcg-sim/pull/212)

- **Primer:** Fourth (and last poll-replacement before presence) loop replaced. `RealtimeServerEvent` now carries `{ type: "game:status"; gameId; status; winnerId; winReason }`. The OPT-356 handoff sketched a worker-side notify, but in practice **all three `GameSession.status` mutators are Next.js routes** (`POST /api/game/result` is the worker → Next callback, plus FINALIZE and CONCEDE in `POST /api/game/[id]`) — no worker-side fanout was needed. New helper `notifyGame(gameId, { status, winnerId, winReason })` loads `player1Id`/`player2Id` from `GameSession` and calls `notifyUser` for each (Solitaire collapses via `Set` dedup). Each mutator schedules `after(() => notifyGame(...))` only when `finalizeGameResult` returns `finalized: true` — re-calls on already-terminal rows are silent. **No backstop poll** per the ticket: status transitions are one-shot terminal, and the one-shot fetch on remount covers any missed push. The 2s `setInterval` in `useRemoteGameStatus` is now a `subscribe("game:status", …)` effect; the consumer recomputes `winnerPerspective` and `canFallbackConcede` per-recipient from `winnerId` + `session.user.id`.
- **Read first:** `prisma/schema.prisma` (the `User` model — OPT-358 adds `lastSeen DateTime?` with an index; this is the project's first schema change in the migration path, so go via `prisma migrate dev --name add-user-lastseen` and ship the SQL in the same PR). `workers/game/src/UserChannel.ts` (the DO that holds open WebSockets — OPT-358 needs `getConnectionCount()` to drive a presence-broadcast on connect/disconnect; `getConnectionCount()` was already exposed in OPT-352 per its handoff). `src/components/social/sidebar.tsx` (or wherever the friends sidebar renders an online dot — confirm with `grep -rn "lastSeen\\|online" src/components`). `src/lib/realtime/serialize-friend.ts` + `src/components/social/apply-friend-event.ts` (the canonical serializer + reducer pattern — clone for `presence:changed` if you go with a per-user event, or roll a different shape if you broadcast set-of-online-userIds).
- **Gotchas / do NOT touch:** **Multi-tab debounce** is the new wrinkle that didn't exist in P1–P3b. A user can have N tabs open; opening one and closing another fires `connect`/`disconnect` against the same DO that should *not* flip the user's `lastSeen` from "online" to "offline." The DO already tracks `getConnectionCount()`; the DB write should only fire when the count crosses 0 (last tab closes) or 1 (first tab opens). Don't write on every connect/disconnect. Do not introduce a new fanout helper — `notifyUser` is the right primitive; presence updates are still per-recipient (each friend gets `{ type: "presence:changed", userId, online }` for a friend whose state changed). The "broadcast to all my friends" pattern needs a friend-list DB lookup *somewhere* — likely in the DO's alarm() or a Next.js helper invoked from the DO's notify-on-state-change webhook. Decide that before coding. Don't gate presence behind connection-status UI — that's been deferred since OPT-353 and the ticket spec calls for `lastSeen` semantics, not "is the WS open right now."
- **Unresolved:** Two-browser manual E2E for OPT-357 **not run** in this session — verified via unit + type + lint. With OPT-355/356/357 all merged-or-merging in aggregate and sharing the same fanout pattern, a single 5-minute multi-browser pass is the right time to confirm all four poll replacements before OPT-358's schema work lands. CodeRabbit/Codex haven't reviewed OPT-357 yet — none of the prior review items touched the game-status path. The `[id]` route's POST handlers (FINALIZE/CONCEDE) still have **no test coverage today** — OPT-357 only added a fanout assertion to `result/route.test.ts`; building out POST coverage in `[id]/route.test.ts` is design debt, scope-skip noted but worth a small follow-up ticket. The `void fetch(...notify-end…)` worker call in `handleConcede` (`src/app/api/game/[id]/route.ts:222-234`) is a separate concern from `notifyGame` — it tells the DO to terminate the in-memory game, not the user-channel push — and intentionally stays as `void fetch` rather than `after()` because it's not blocking on Next.js handler completion.
- **Why this matters for OPT-358:** OPT-358 ships **real presence** — the first feature in this project that's not just deleting a poll. It's also the first **schema change** (User.lastSeen) and the first ticket where the trigger source is a **Durable Object lifecycle event** (WS connect/disconnect), not an HTTP route. The fanout pattern is mostly a copy of friends (per-user `notifyUser` to each friend), but the multi-tab debounce + DB write economy are the two new design problems. Use the OPT-357 helper as a model for `notifyPresence(userId, online)`: load the friend list, fan out via `notifyUser`. After OPT-358, OPT-359/360 (typing, lobby invites — stretch features) and OPT-361 (cleanup of all 60s backstops, gated on a 7-day soak window) are the remaining tickets.

### OPT-359 → OPT-360
**From:** session on 2026-05-03 · **Commit:** `50ebac0` · **PR:** [#214](https://github.com/corycunanan/optcg-sim/pull/214)

- **Primer:** First **client→server** event vocabulary on the realtime channel. `RealtimeClientEvent` is the new discriminated union (mirror of `RealtimeServerEvent`); `useUserChannelEvents().send(event)` is the surface, threaded through `useUserChannel` → `<UserChannelProvider>` from `useAuthedWebSocket.send`. The DO's `webSocketMessage` handler (previously a drop-and-log) now JSON-parses, hand-rolled-validates the discriminator, and routes — `chat:typing` is the first variant. Inbound throttle is per-DO at `TYPING_THROTTLE_MS = 1.5s`; `until` is clamped to `now + TYPING_MAX_UNTIL_MS = 10s`. Forward to recipient is the same DO-to-DO `env.USER_CHANNEL.idFromName(toUserId).fetch(.../notify)` pattern OPT-358 introduced — no Next.js round-trip. Read receipts: `Message.readAt DateTime?` schema change (kept in sync with `read` boolean for backward compat — the `/api/messages/conversations` unread-count subquery is unchanged), new `POST /api/messages/[userId]/read` endpoint replaces the implicit GET-marks-read side-effect. Sender-side ✓ vs ✓✓ rendering hangs off `ChatMessage.readAt`; `applyReadToEvent` reducer handles `chat:read_to` events.
- **Read first:** `src/types/realtime.ts` (the new `RealtimeClientEvent` union — extend it for OPT-360 if invites need a client→server signal like "viewed"; otherwise stick to server-side `RealtimeServerEvent`). `workers/game/src/UserChannel.ts` `webSocketMessage` + `handleTyping` (the canonical client→server handler — clone shape for any future inbound vocabulary). `src/components/realtime/user-channel-provider.tsx` (now exposes `send`). `src/components/social/chat-typing-state.ts` (pure-helper extraction pattern for client-side throttle / window logic — useful template if OPT-360 needs lobby-invite-acceptance windows). `src/app/api/messages/[userId]/read/route.ts` (the canonical `notifyUser`-on-success-via-`after()` pattern for *replacing* a side-effect rather than adding one). `src/components/social/apply-message-event.ts` `applyReadToEvent` (the read-state reducer — pure, idempotent on already-set `readAt`).
- **Gotchas / do NOT touch:** **The `DO_NOT_REUSE_FRIENDS_CACHE_FOR_TYPING` rule from OPT-358's handoff still applies** — `chat:typing` fans to the conversation partner directly, no friend-list step. Don't extend `fetchFriends` for it. The chat-widget's `lastTypingEmitRef` initial value is `NEVER_EMITTED` (`Number.NEGATIVE_INFINITY`); using `0` would make the first keystroke fire iff `Date.now() >= 1000` — fine in production, broken in tests with `vi.setSystemTime(0)`. Don't change it. The `applyReadToEvent` reducer is **non-overwriting** — it skips messages whose `readAt` is already set. This matters because the new-message effect re-fires `markAsRead` on every incoming message, and a backstop poll might land a fresh `readAt` from the server before the local push reducer catches up; preserving the earlier timestamp keeps the receipt UI monotonic. The DO drops messages > `MAX_CLIENT_MESSAGE_BYTES = 8KiB`; if OPT-360 adds a fatter inbound event, raise the cap consciously, don't accidentally let a giant payload hibernate the DO. The `read` boolean in `Message` is **dual-written** by the new POST `/read` route and by older code paths; if you remove the column in a future cleanup, audit `/api/messages/conversations` first — it filters on `read = false` for the unread count.
- **Unresolved:** Two-browser manual E2E (typing-indicator latency, ✓✓ timing) **not run** in this session — verified via worker `opt-359-typing.test.ts` (5 cases) + chat-widget pure-helper tests + route tests + `applyReadToEvent` reducer tests. Worth a single 5-minute pass before merging. `db:check-migration-drift` skipped locally (no `SHADOW_DATABASE_URL` configured); CI catches it. The `NEVER_EMITTED` sentinel pattern could move to a shared `realtime/throttle.ts` if a second feature needs the same gate — not worth it for one consumer today.
- **Why this matters for OPT-360:** OPT-360 ships **lobby invite notifications** — the only remaining feature ticket before the OPT-361 cleanup. Unlike OPT-359, invites are **server-side** events (`POST /api/lobbies/.../invite` → `notifyUser(targetUserId, { type: "lobby:invite_received", ... })`) so the new `RealtimeClientEvent` machinery isn't needed; just extend `RealtimeServerEvent` and follow the OPT-354/355 pattern. The receiving UI surface is **not** the chat-widget — it's a notification toast or inbox tray, design TBD. Persistence question to decide: does an unaccepted invite live in a DB table (so it survives the recipient being offline), or only in-flight via the channel (lossy if recipient's tab is closed)? Lean toward DB persistence — the existing 60s backstop polls only carry mass-mutation reconciliation, not point-in-time invite arrivals, so a missed push = lost invite without a backing row. After OPT-360, OPT-361 (cleanup of all 60s backstops, gated on 7-day soak after OPT-358 merge on 2026-05-02) is the only remaining ticket.

### OPT-360 → OPT-361
**From:** session on 2026-05-03 · **Commit:** `bdda51d` · **PR:** *(opens with this commit)*

- **Primer:** First non-poll-replacement feature. New `LobbyInvite` row (UUID, 5-min TTL, status enum `PENDING|ACCEPTED|DECLINED|EXPIRED|CANCELED`) + three event variants (`lobby:invite_received`, `lobby:invite_declined`, `lobby:invite_canceled`). Four routes: `POST /api/lobbies/[id]/invite` (host-only, friend-only, `socialLimiter`, dedup on `(lobbyId,toUserId,PENDING)`), `POST /api/lobby-invites/[id]/accept` (recipient gate, mirrors `POST /api/lobbies/join`'s seat-+-status-+-fanout choreography by id rather than code), `POST /api/lobby-invites/[id]/decline` (fans to host **and** recipient — multi-tab echo), `GET /api/lobby-invites/pending` (reconciliation: same wire shape as `lobby:invite_received`, scoped to `expiresAt > now` + `lobby.status in {WAITING, READY}`). Sender UI is `InviteFriendPopover` next to the join code in PVP lobbies (host-only). Recipient is `<LobbyInviteToasts />` mounted in `social-shell.tsx` outside the `isGame` gate so toasts surface even on `/game/*`.
- **Read first:** `src/lib/lobbies/cancel-invites.ts` (the choreography for "lobby state moves on; pending invites die" — both DELETE and start routes call it via `after()`; clone the shape if OPT-361 needs to fan over a list during cleanup). `src/components/lobbies/lobby-invite-toast-state.ts` (pure reducer pattern — reference if anything else needs the same in-app toast queue). `src/types/realtime.ts` (full `RealtimeServerEvent` union — three new variants live next to the rest). `src/app/api/lobby-invites/pending/route.ts` (the canonical reconciliation-fetch shape — useful template if any per-recipient state ever needs a similar GET).
- **Gotchas / do NOT touch:** `Lobby.invites` cascades on hard-delete, but **the app soft-deletes** lobbies (status → CLOSED) — `cancelPendingLobbyInvites()` is the explicit fanout site, don't rely on cascade. Decline fans to **both** the host (clear "Invited X" state) **and** the recipient (multi-tab echo) — keep the dual fanout. The `LobbyInviteToasts` shell now renders even on `/game/*` (the `isGame` gate was lifted *only* for the toast subtree); don't reintroduce the gate around it. The `InvitePanel` shows the popover only when `showInviteFriend={isHost}` — the guest doesn't get an invite button (would create cross-talk on who's authoritative). The 5-min TTL is a constant (`INVITE_TTL_MS`) in the invite POST route — if OPT-361 cleanup also wants to drop this, that's a separate ticket. The `apiPost`/`apiGet` callsites use `type` aliases (not `interface`) for response shapes because TypeScript's `Record<string, unknown>` constraint on the helper rejects interface types without an index signature — keep that pattern.
- **Unresolved:** Two-browser manual E2E (A invites B → toast within 1s → Join → in lobby in <2s; cancel-on-start dismisses C's toast) **not run** in this session. Verified via 482 unit/route tests + type-check + lint. Worth a single 5-minute pass before merging this PR alongside OPT-359's two-browser pass. `db:check-migration-drift` skipped locally (no `SHADOW_DATABASE_URL`); CI catches it. The `lobby:invite_canceled` recipient echo is best-effort — if a recipient tab never receives the push, the toast self-clears at TTL anyway. Decline doesn't currently rate-limit on `socialLimiter` (uses `apiLimiter`); consider tightening if abuse is observed.
- **Why this matters for OPT-361:** OPT-361 deletes the 60s backstop polls in chat-widget, social-sidebar, and `useLobbyRoom`. The OPT-360 path doesn't add a backstop — invites are entirely event-driven plus the on-mount `GET /pending` reconciliation, which is fundamentally different from the periodic polls and **does not need to be removed by OPT-361**. The cleanup ticket is gated on a 7-day soak window after OPT-358 merge (2026-05-02) — earliest unblock is 2026-05-09. Coordinate the merge order: OPT-359 + OPT-360 PRs land first, then a soak observation window, then OPT-361 lands as a one-shot mechanical removal of three `setInterval(60_000)` calls.

### OPT-358 → OPT-359
**From:** session on 2026-05-02 · **Commit:** `03a5d6f` · **PR:** [#213](https://github.com/corycunanan/optcg-sim/pull/213)

- **Primer:** Real friend presence shipped. The DO now drives `presence:friend_online` on count 0→1 and `presence:friend_offline` on count N→0 after a 5s debounce; a re-attach inside the window cancels the timer so multi-tab flips never flicker. Fanout to friends goes **DO-to-DO** via `env.USER_CHANNEL.idFromName(friendId).fetch("https://internal/user/.../notify", { ... })` — the existing `/notify` route on each `UserChannel` instance accepts the same Bearer secret it's always accepted, so no new internal protocol. The DO can't talk to Postgres, so two new Next.js routes proxy what it needs: `GET /api/realtime/friends-of/[userId]` (Bearer-authed, friend-list lookup, cached in the DO for 60s) and `POST /api/realtime/users/[userId]/last-seen` (Bearer-authed, idempotent best-effort write). Initial seed for the client comes from `GET /api/users/presence?ids=...` which filters to friend ids only (presence-leak gate), batch-loads `User.lastSeen`, and parallelises worker `/health` for the online count. Provider exposes `presence: PresenceMap` + `trackPresence(ids)`; the sidebar registers its friends and the "Online (N)" label now counts actually-online friends.
- **Read first:** `src/types/realtime.ts` (the union — extend with the new client→server vocabulary OPT-359 introduces; existing variants are server→client only, so the discriminator pattern carries through but the dispatcher's direction inverts). `src/components/realtime/user-channel-provider.tsx` + `src/components/realtime/presence-state.ts` (the seed + reducer pattern; pure reducer + provider-level state is the right shape if OPT-359 adds another aggregated map like `typingByConversation`). `workers/game/src/UserChannel.ts` (the DO — OPT-359's first client→server vocabulary lands its handler in `webSocketMessage`; today it drops with `user_channel.message_dropped`). `src/components/social/chat-widget.tsx` (the canonical chat consumer — OPT-359 typing UI hangs off the same component). `src/app/api/realtime/friends-of/[userId]/route.ts` and `src/app/api/realtime/users/[userId]/last-seen/route.ts` (the Bearer-authed worker callback pattern — clone if OPT-359 needs a similar proxy).
- **Gotchas / do NOT touch:** **OPT-359 is the first client→server vocabulary** — `UserChannel.webSocketMessage` currently logs and drops, and `MAX_CLIENT_MESSAGE_BYTES = 8 * 1024` is the existing safety cap. Validate every inbound shape (Zod or hand-rolled discriminated union) before routing — no schema, no acceptance. The presence broadcast pattern uses `env.USER_CHANNEL.idFromName(friendId).fetch()` directly to fan typing indicators to a chat conversation's other party — but **typing has no friend-list step**, the recipient is just the other end of the conversation; don't reuse `fetchFriends` for it. The 60s in-DO friends cache (`FRIENDS_CACHE_TTL_MS`) means a freshly-added friend won't see presence flips for up to a minute; if OPT-359 adds another DO-side cache, give it a clearly different name and TTL — don't share the same lookup. The `OFFLINE_DEBOUNCE` timer is a `setTimeout`, not an alarm; CF DOs only support one alarm at a time and idle-reap already owns it. Don't replace the timer with the alarm. Lastly: the multi-tab flicker test is the load-bearing assertion for OPT-358 (`closing one of two tabs does NOT schedule offline`) — if OPT-359's typing handler triggers extra fanout work on connect/disconnect, run it through that same gate.
- **Unresolved:** Two-browser manual E2E for OPT-358 **not run** this session — covered by the worker test (`opt-358-presence.test.ts`, 9 cases incl. multi-tab) and the Next.js tests, but the spec calls for an A-and-B browser pass to confirm green-dot timing. The 5s debounce is a magic constant — happy to tune if soak data shows 5s is too aggressive (a Wi-Fi flap is ~3s; 5s is comfortable but not generous). The `usePresenceMap` API exposed via the provider is `trackPresence(ids)` — callers register but never unregister; the tracked-id set grows with the user's session. For typical friend-list sizes this is fine, but if OPT-360 surfaces a "lobby viewers" presence list with churning ids, the set will need GC.
- **Why this matters for OPT-359:** OPT-359 (typing indicators + read receipts, P5a/b) is the **first client→server event vocabulary**. Until now the channel has been one-way: server pushes events, the client subscribes via the dispatcher. OPT-359 inverts that for two events — `typing:start` and `read_receipt:ack` (names TBD). The DO's `webSocketMessage` handler today drops everything; OPT-359 needs to (1) validate the shape, (2) route to a recipient via `env.USER_CHANNEL.idFromName(otherUserId).fetch(.../notify)`, and (3) optionally PATCH a DB write (read receipts). The fanout primitive is the same one OPT-358 just used; the only new design surface is the validation + routing inside the DO. The friend-list lookup is **not** needed (typing/receipts are 1:1 within a chat, not 1:N to a friend list).
