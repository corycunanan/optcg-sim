# OPT-810 — Leader alternatives and next paid hand-play scope

Base main `15cad98bd139fc0d8cea075c809e4f8a7b45f507` (remote rechecked2026-09-10). Exclusive branch `codex/opt-810-card-fidelity`, clone `/private/tmp/optcg-opt810`. Recovered predecessor checkpoint90059ed; preserved its four card fixes and completed behavior/persistence coverage. No sibling unmerged commits imported. Implementer never merges; the coordinator now has user authorization for non-frontend merges in this run. Final committed SHA and gate results belong in the PR body.

## Sources and resulting behavior

Read canonical `docs/cards/OP-11.md`, `OP-12.md`, OP01 Bepo and OP06 Moria text, and official OP12 FAQ `docs/FAQs/qa_op12.md` Rosinante entry. Official card-list pages [OP11](https://en.onepiece-cardgame.com/cardlist/?series=569111), [OP12](https://en.onepiece-cardgame.com/cardlist/?series=569112), [OP01](https://en.onepiece-cardgame.com/cardlist/?series=569101), [OP06](https://en.onepiece-cardgame.com/cardlist/?series=569106), checked2026-09-10, establish printed metadata. Supported rules v1.2.0 distinguish continuous effects, paid play, effect play, and once-per-turn activation.

- Jinbe OP11-031 uses explicit `any_of` LEADER_PROPERTY clauses for Fish-Man or Merfolk. Actual cost6 play rests the selected cost5 opponent Character; cost6 is excluded, zero selection changes neither Character, and an unrelated Leader trait prevents the prompt.
- Robin OP12-087 uses the same explicit disjunction for Koala or Monkey.D.Luffy. Actual cost6 play gets both Blocker and +3 field cost (9) for either name; unrelated name gets neither. Its separate optional On Play is declined through its real prompt.
- Megalo OP11-112 removes only the unprinted ACTIVE-state restriction. Printed2000 power becomes6000 on the opponent's turn with Shirahoshi, active or rested; actual attack→rest→turn change proves the rested transition. Own turn and wrong Leader remain2000.
- Rosinante OP12-061 creates a PLAY_CARD/HAND-scoped next-play modifier, preserving name, printed cost≥4, −2 amount, once-per-turn and this-turn duration. Actual first matching paid hand play pays2 for cost4; second pays4. Unrelated Character or cost3 Law pays full and does not consume it. A second activation has no additional effect or cost (the pipeline accepts a no-op rather than returning validation failure). End-of-turn expiration restores full cost.

The predecessor's ten-case fixtures used synthetic Jinbe/Robin costs and Megalo power; final tests correct these to official6/6/2000. Law cost3/4 boundary targets and generic opposing targets deliberately use simple CardData fixtures: all behavior under test comes from the real registered Rosinante/Jinbe/Robin/Megalo, Bepo, Moria or Kinemon schema. These are engine fixtures, not deck legality tests.

## Bounded shared support and inventory

Coordinator approved adding optional `source_zone: HAND` and PLAY_CARD scope to RuntimeOneTimeModifier, with identical quote/consumption matching against the original instance before movement. `getEffectiveCost` gains a default PLAY_CARD context; `executePlayCard` supplies the original instance to `consumeOneTimeModifiers`. Counter validation and execution explicitly identify USE_COUNTER_EVENT. Unknown action/source scopes fail closed. Legacy absent action and MODIFY_COST remain supported; replacement modifiers do not enter MODIFY_COST matching. Removed an accidental unused costAction parameter from the layer2 helper and corrected its stale comment. No broad one-time-modifier redesign.

Recursive production registry inventory has exactly three APPLY_ONE_TIME_MODIFIER nodes (`/private/tmp/opt810-producer-inventory.txt`): OP02-025 Kinemon legacy MODIFY_COST; OP12-061 Rosinante scoped MODIFY_COST; EB02-030 replacement protection. No sibling producer changed. `executeApplyOneTimeModifier` remains the producer in effect-resolver/actions/effects.ts; modifications and applies_to are stored together unchanged.

All non-test getEffectiveCost call sites: validation.ts paid play; execute.ts paid play/sole cost-modifier consumption; validation.ts and battle.ts counter Events (explicit counter context); visibility.ts own-hand payable quote (default paid hand play). modifiers.ts getEffectiveFieldCost and getEffectiveCostForRead disable play-time modifiers. The latter feeds target-resolver filters and condition-queries via conditions.ts query adapters. Existing OPT444/450 cases cover these property/read boundaries. Cleanup/expiry remains in modifiers.ts. Opaque modifier scope is copied by history/visibility and staged cost state, without new wire fields or migrations.

Persistence inventory: persisted-game-state.ts accepts oneTimeModifiers as z.unknown array; persistence.ts validates each modification, not a strict appliesTo shape. That is an existing opaque contract, not a claim of newly strict scope validation. Actual SessionRepository.save followed by a fresh repository.load round-trips active Rosinante scope, hand quote, unknown future scope and pending free-play choices. Durable prompt IDs generated by persistence are echoed through SessionCoordinator's actual response gate before resuming.

Official Rosinante FAQ says free effect play from hand does not consume the discount. Authored Bepo's When Attacking effect, with one DON!! attached through ATTACH_DON, plays the selected cost4 Heart Pirates Law from hand for free after a persisted prompt; the subsequent paid Law still costs2. Authored Moria is paid8 with a valid ten-DON starting fixture, plays Law from trash for free after persisted target/state-choice prompts, and leaves the next hand Law quote2. Both observe fresh field identity and unchanged DON payment beyond printed activation/play costs. Legacy authored Kinemon still discounts exactly its first qualifying Wano play after repository persistence.

## Validation

Shared baseline caf29b1: `/private/tmp/opt811-baseline-verify.log` passed lint/types/bundle/schema, app2360+7existing skips, pipeline48, worker2458+5existing skips,83.27% statements; sandbox font fetch was resolved by elevated build in `opt811-baseline-build.log`. This is shared evidence, not a new local baseline.

Initial predecessor10-case red run failed4 (`opt810-red.log`). Final suite has22 cases and passes with corrected metadata (`opt810-expanded-final.log`); worker types pass (`opt810-final-types.log`). Nine semantic mutations each run all22 cases: original Jinbe3fail, Megalo2, Robin1, Rosinante2; missing source check1, action/controller check1, consumption match5, Law minimum1, unknown-action rejection1. Script `/private/tmp/opt810-mutations.py`, results `/private/tmp/opt810-mutations.json`, adjacent logs. Restored source/generated registry after every mutation. Restored focused83 cases across six suites pass (`opt810-final-focused.log`); schema gate passes34 tests,2472 cards and generated parity (`opt810-final-schema.log`). No new skipped tests.

One existing direct-consumption fixture now supplies the required original HAND instance for its explicit PLAY_CARD scope; the consumed assertion remains intact, and production fails closed without an instance. Full gate will run at the final committed head. Independent review, coordinator final diff review, publication and current CI remain required; this document is implementation evidence, not merge-ready status.

## Follow-ups

No new prerequisite. Ticket exclusions remain OPT-801 (OP11-041), OPT-797 (OP11-101), OPT-804 (ST21-003/OP12-016/OP12-077). Counter-context protection is directly asserted on the shared payable-cost API and both production counter readers explicitly supply that context; no printed Law is a Counter Event, so the card-specific fixture does not invent one. Broader one-time-modifier semantics outside the inventoried producers remain unchanged.
