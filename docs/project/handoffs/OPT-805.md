# OPT-805 — authored Crocodile and deck search fidelity

Workspace `/private/tmp/optcg-opt805`, branch `codex/opt-805-card-fidelity`, base main `15cad98bd139fc0d8cea075c809e4f8a7b45f507`. Isolated from unmerged siblings. The review delta adds bounded root Event activation lifecycle support described below. Stop at merge-ready; never merge or write Linear. Final tested SHA, full-gate outcome, and readiness status are recorded in the PR body/coordinator receipt after this committed handoff.

## Source-driven scope correction

The proposed OP02-071 Magellan SELF filter conflicts with the official FAQ. The live [Bandai OP02 FAQ](https://en.onepiece-cardgame.com/pdf/qa_op02.pdf?20240405=), page10 (PDF index9), rows OP02-071 explicitly permits opponent DON returned to the opponent's deck, and says a simultaneous two-DON return gives only1000 power. OP02-085 Character is a separate later row; this is not a parsing mix-up. Local `docs/FAQs/qa_op02.md` agrees. Coordinator independently confirmed the source and appended/audited a correction to OPT805 (comment ID `ff3aa364-b006-4412-b869-cbde57f682aa`). **Preserve broad matching; Magellan criterion is verified-no-change plus regressions.** An inline schema comment explains why there is no controller restriction.

Other sources: `docs/cards/OP-01.md`, `OP-02.md`, `ST-03.md`; `docs/FAQs/qa_op01.md` (Crocodile checks hand after Event resolution, triggers excluded, no draw-used flag until draw), `qa_op02.md` (Oden exactcost3 and mandatory shuffle even if no play), `qa_st-01-st-04.md` (Sentomaru activation with no Pacifista still searches/shuffles). Comprehensive Rules v1.2.0 §§1-3-5-1 (zero for up-to),3-1-6 (fresh zone identity),8-3-1/8-3-2 (costs/conditions),8-6 (effect ordering).

## Acceptance → behavior → regression

| Card/clause | Implementation / evaluation | Observable proof |
| --- | --- | --- |
| OP01-062 any own Event, DONx1, hand≤4, draw once | Remove unprinted cost_reduced and use hand/trash-main any_of pair; DON requirement on each branch; existing condition/optional draw/once flags retained | Actual SMILE full-cost Event pipeline draws at post-Event hand3/4; hand5/noDON do not. ST04-014 first draws to5 so Croc does not. Decline then accept then third Event gives only one draw. |
| OP01-062 Event Main from trash, not Life Trigger | Registered Crocodile exact event matching; actual Reiju/Sanji pipeline publishes trash-main event after SMILE resolves | Positive SELF trash matching, opponent/noDON/Trigger negatives. **No invented legal Crocodile/Reiju combo:** only authored ACTIVATE_EVENT_FROM_TRASH source EB03-031 requires a Sanji Leader, so end-to-end trash-publication and Crocodile matching are distinct proofs; complete Crocodile trash activation in a legal current card combination is not claimed. |
| OP02-071 broad DON return per FAQ, your turn/once | Preserve existing broad trigger; no SELF filter | Own Sheep's Horn returns twice →6000 once. Opponent Ulti-Mortar returns2 during your attack →6000, not7000. Same return on Magellan's opponent turn →5000. |
| OP01-069 up-to1 Smiley onKO, then shuffle | Explicit `pick:{up_to:1}` | Authored Kaido K.O. pipeline → search: zero/one, max2 rejection, wrong-name rejection; full deck preserved/shuffled, fresh played identity. |
| OP01-116 top5 up-to1 SMILE Character cost≤3, rest bottom anyorder | Explicit pick | Actual Main Event pipeline → zero/one; max2 and cost4 decoy rejected; remaining revealed cards placed at chosen bottom order; outside top5 remains present. |
| OP02-030 onKO up-to1 Green/Wano/exactcost3, then shuffle | Explicit pick | Kaido K.O. → zero/one, max2 and matching-trait cost2 decoy rejected; deck shuffled even at zero. |
| ST03-007 DONx1/ActivateMain/once, rest2DON cost, up-to1 Pacifista≤4, shuffle | Explicit pick | Real ActivateEffect pays2DON; zero/one; max2 and Pacifista5 rejected; no matches still resolves/shuffles. |

Every search uses registered production schema, actual pipeline triggers or activation, all arrange prompts, SessionCoordinator prompt-response validation, and resumePromptLifecycle. The four search cards already used an effective default maximum1 and allowed zero **before this change**. Explicit pick is an encoding clarification, not a falsely claimed resolver fix. OPT772 was correctly treated as SEARCH_DECK/SEARCH_TRASH support only; direct SEARCH_AND_PLAY execution established current behavior.

## Validation evidence

- New authored suite27passed; corrected legacy Crocodile classification suite8passed; combined35passed (`/private/tmp/opt805-focused.log`).
- Original main implementation with authored27-case suite:4failed/23passed (`/private/tmp/opt805-baseline.log`): full-cost Crocodile positives, decline/retry, trash branch. Search and Magellan baseline behaviors already pass, honestly recorded as such.
- Wrong proposed Magellan SELF-filter mutation: opponent Ulti-Mortar case fails (1failed/1passed; `/private/tmp/opt805-magellan-mutation.log`); restored schema/regenerated registry and35green. Thus the official FAQ expectation is independently falsifiable.
- `pnpm test:pipeline`:48passed (`/private/tmp/opt805-pipeline.log`). `pnpm schema:check`:2472schemas,34tests passed (`/private/tmp/opt805-schema.log`). Worker typecheck passed during implementation (`/private/tmp/opt805-type.log`).
- Required final `pnpm verify` is run against the committed source/handoff; exact SHA, exit, counts, and any environment limits are recorded in the PR body. Full base gate was not run. No merge-ready assertion is made by this handoff alone.
- Production schema registry regenerated from owned op01/op02/st03 sources. No hand-deck handler, protocol, or unmerged sibling code imported. Existing opt238 tests/docs that incorrectly attributed cost reduction to Crocodile were corrected against its printed text/FAQ; generic cost-reduced vocabulary remains unchanged.

## Follow-ups and limits

- Preserve ticket-listed OP01-063 Arlong approximation: printed “up to1” opponent Life→deck clause still uses fixed LIFE_CARD_TO_DECK amount1. Out of scope here.
- Crocodile trash branch legal-combination limitation is explicit above; publication and matching proofs are separate. No fake Leader rename or bypass of Reiju's Sanji condition was used.
- Current source/PR base, required external checks, independent/coordinator review, and final readiness remain coordinator gates. No runtime migrations or deployment steps are introduced.


## Review P1 correction: Main and Counter activation lifecycle

The original 712d293 review was superseded by two reproduced P1 findings: Counter Events emitted only COUNTER_USED, and prompted Main Events lost outer activation events before Step5. Actual Ulti-Mortar and legal SMILE zero/one searches produced three failures (`/private/tmp/opt805-p1-red.log`). These are addressed in this delta; final reviewed SHA/gate receipt belongs in PR659 body.

Read `battle.ts`, `execute.ts`, `pipeline.ts`, `session/prompt-lifecycle.ts`, persistence and projection changes, then the added authored regressions. Counter blocks now resolve directly like Main blocks before activation watchers are matched. Character counters remain excluded. Root CARD_PLAYED/COUNTER_USED/activation prefixes are published before nested Event results using the existing immutable eventLogEmitted fact; publication does not scan watchers. Prompted root executions retain only outer pending propagation debt not already owned by resolver frames in optional private pendingEventActivationEvents. Once the Event stack fully completes, lifecycle clears that debt before Step5 scans it. The committed root action is recorded once before pause; resume is not a second play. Concede/engine termination discard the debt. No new event variant or public protocol field is introduced.

Persisted core validates a strict optional event-envelope array (including literal propagation flags); the existing pending-event validator rejects unknown discriminants and strict payload failures. Legacy snapshots without the field still load. Player filters delete the private field; spectator exhaustive projection deliberately omits it. Setup constructs new state without debt. Coordinator undo stores/restores whole snapshots, history compaction spreads state, and SessionRepository serializes/restores current and undo states through the same validator. Cost sandbox transaction overlays only its seven staged fields onto live state, preserving private execution metadata. Engine limit/contract termination, concede and ordinary defeat clear debt; prompt lifecycle discards it on terminal completion. No lossy queue copy was found in these readers.

Recursive registry inventory (2472 registered cards): hand/trash activation watchers are OP01-004 Usopp, OP01-062 Crocodile, OP04-053 Page One, OP06-044 Gion, OP06-048 Zeff, OP10-003 Sugar, OP11-012 Franky, OP11-102 Camie, OP15-119 Luffy. No authored COUNTER-keyword watchers and no card with multiple COUNTER_EVENT blocks exist; direct counter-block selection preserves current authored cardinality. Actual Usopp and Franky Counter activation regressions exercise opposing and same-player ordering with Crocodile, including persistence through the ordering prompt. Other authored watcher encodings are unchanged.

Publishers/readers: root Main execute.ts and Counter battle.ts; effect actions play.ts (hand and trash); pipeline Step5, custom event matcher, public event visibility, strict event payload decoder. Effect activators are EB03-031 (trash) and OP12-041/OP15-014/OP15-046 (hand). Existing resolver frame propagation remains unchanged, and OPT236/237/238/820 suites pass alongside new root tests. The hand effect-activation handler currently moves/emits without directly resolving Main; this pre-existing separate path is not implemented or claimed complete by the root barrier change. It requires separate investigation, as does nested trash activation timing if expanded coverage finds a problem.

Expanded proof: 39 authored pipeline cases, including JSON-backed SessionRepository save/fresh-load at Main search, Counter cost/target, watcher-order and Crocodile prompts. Tests assert post-effect hand2/4 positives vs draw-to5 negative, declined Event cost, Crocodile decline/retry/once, Character-counter exclusion, no-schema Counter completion, terminal concede, legacy decode and malformed new queue rejection, private player/spectator views, and one root publication/action/trigger after reload. Exact root-before-nested event ordering is asserted for SMILE play and Counter draw. Eight focused suites115 passed including all39 authored cases; final full counts are recorded in PR body. Full verify is renewed after this source/test/handoff commit; no merge-ready claim until independent delta review and coordinator gates.
