# M2.5 — Design System

> Establish a consistent, tight design system grounded in the One Piece brand. Fix spacing, typography, color, and styling discipline before building the social layer and game board.

---

## Why This Phase Exists

The M1 deck builder revealed systemic design debt:

- **100+ mixed styling instances** — components use both inline `style={{}}` and Tailwind classes simultaneously, making global changes fragile
- **15+ hardcoded oklch() values** — color decisions scattered across components instead of living in tokens
- **8+ custom font sizes** (`text-[9px]`, `text-[10px]`, `text-[11px]`) — no real typographic scale
- **20+ spacing values off the 8px grid** — random `p-2.5`, `px-3 py-1.5`, `pt-4`
- **6 different border-radius values** without clear hierarchy
- **Decorative effects** (backdrop blur on badges, lift-on-hover stacking) that feel gamified rather than premium
- **Inline style manipulation via JS** (`onMouseOver` setting `borderLeftColor`) instead of CSS state

The current aesthetic direction (dark, moody, teal-tinted) doesn't match what users actually expect from a One Piece tool. The official OPTCG website and the franchise itself are bright, energetic, and warm — not somber. We're building for One Piece fans, not for a generic esports platform.

This phase ships before M2 (Social) so that every subsequent surface is built on a solid foundation.

---

## Design Direction

### References

- **Primary:** [Official OPTCG Website](https://en.onepiece-cardgame.com/) — bright white/off-white surfaces, deep navy navigation, gold accents, generous whitespace, card art as hero
- **Secondary:** [MTG Arena](https://magic.wizards.com/en/mtgarena) — sophisticated spacing, fluid typography, purposeful motion, CSS variable discipline

### Brand Personality (Revised)

**Adventurous. Warm. Confident.**

One Piece is about joy, camaraderie, and the pursuit of dreams — not grim darkness. The OPTCG Simulator should channel that energy. Card art is spectacular and deserves a clean, bright stage. The UI should feel like the official OPTCG site: professional, alive, and unmistakably One Piece.

Emotional goals: delight when browsing cards, focus when building decks, immersion during gameplay.

### Aesthetic Direction (Revised)

- **Visual tone:** Clean and bright, with purposeful dark moments (game board, modals). Inspired by the official OPTCG website — professional, welcoming, high-fidelity card presentation.
- **Primary theme:** Light mode. Dark mode surfaces reserved for the game board where immersion matters most.
- **Color palette:**
  - **Base:** Warm white (`#FAFAF8`) and off-white (`#F5F4F0`) backgrounds — not pure white
  - **Navy:** Deep navy (`oklch(22% 0.04 245)`) for navigation, headers, primary text — the franchise's structural color
  - **Gold:** Warm amber/gold (`oklch(72% 0.14 75)`) for primary accents, highlights, CTAs — treasure, adventure
  - **Red:** One Piece red (`oklch(55% 0.20 25)`) for emphasis, energy, destructive actions
  - **Card colors:** The six TCG colors (Red, Blue, Green, Purple, Black, Yellow) remain functional, not decorative
- **Typography:**
  - **Display/headings:** Barlow Condensed (Google Fonts) — bold condensed for page titles and section headers. Weights 600–800.
  - **Body:** Geist Sans — all UI text, labels, body copy.
  - Scale: 12/14/16/18/20/24/30px only. No custom `text-[Xpx]` in components.
- **Accessibility:** WCAG AA — 4.5:1 contrast for text, keyboard navigable, focus visible.
- **Card presentation:** Cards are the hero. Clean warm-white surfaces let art breathe. Hover states are subtle — no stacking of lift + shadow + blur simultaneously.
- **Anti-references:** NOT dark/moody/gamer. NOT generic SaaS dashboard. NOT neon-on-black. NOT Moxfield's utilitarian grid. The OPTCG official site's warmth and the MTG Arena site's polish — that's the target.

### Design Principles (Revised)

1. **Card art is the hero** — Clean backgrounds amplify artwork. UI chrome is minimal and recedes.
2. **One Piece warmth** — Bright, energetic, inviting. Warmth comes from color and whitespace, not decoration.
3. **Tight system, loose expression** — Every spacing, type, and color decision traces back to a token. Within that system, components can be expressive.
4. **Motion earns its place** — Transitions communicate state, not personality. One clear animation per interaction.
5. **Progressive clarity** — Simple at rest, detailed on interaction. Dense information (stats, filters) is scannable, not overwhelming.

---

## Scope

### Deliverables

**1. Design Token System (globals.css)**
- [x] Revised color palette: warm white base, navy, gold, red, semantic (success/warning/error)
- [x] Strict spacing scale: 4px increments
- [x] Border-radius system: `rounded` (4px), `rounded-md` (8px), `rounded-lg` (12px), `rounded-full`
- [x] Typography scale: xs/sm/base/lg/xl/2xl/3xl only
- [x] Overlay token: `--overlay` for modal backdrops
- [x] Semantic alpha variants: `--error-soft`, `--warning-soft`, `--success-soft` as tokens
- [x] Tailwind v4 `@theme inline` expansion: all tokens exposed as Tailwind utilities

**2. Styling Discipline Rules**
- [x] No inline `style={{}}` for design tokens across all page files and deck builder components
- [x] No hardcoded oklch/hex values in component files
- [x] No custom `text-[Xpx]` sizes — `text-xs` minimum enforced
- [x] Spacing scale enforced throughout
- [x] No JS-based style manipulation
- [x] `cn()` utility used for all conditional classes

**3. Base Component Library (`src/components/ui/`)**
- [x] `Button`
- [x] `Badge`
- [x] `Input`
- [x] `Select`
- [x] `Dialog`
- [x] `Tooltip`
- [x] `Tabs`
- [x] `Toast`

**4. Existing Component Cleanup**
- [x] deck-builder-search: inline styles removed, spacing fixed, touch targets 44px
- [x] deck-builder-list: JS mouseover removed, colored border-l removed, spacing fixed
- [x] deck-builder-header: padding fixed, Barlow Condensed applied to deck name
- [x] deck-builder-stats: refactored to `DeckBuilderStatsCharts`, moved to Stats tab
- [x] deck-builder-validation: rewritten as compact inline tags (X/50 + rule pills)
- [x] card-inspect-modal: Dialog primitive, inline styles removed, Barlow Condensed on card name
- [x] card-grid: gradient overlays removed, font sizes fixed, rounded-lg restored
- [x] export-modal / import-modal: Dialog primitive

**5. Typography Pass**
- [x] All `text-[9px]`, `text-[10px]`, `text-[11px]` removed
- [x] Font weights standardized
- [x] Barlow Condensed (`font-display`) applied to page titles, deck name, card name in modal

**6. Update CLAUDE.md**
- [x] Aesthetic direction updated to One Piece warmth
- [x] Styling rules documented and enforced

**7. Deck Builder UX (bonus scope)**
- [x] Cards/Stats tabs added to right panel
- [x] Leader card enlarged (w-48), clickable → inspect modal with art variant selection
- [x] Compact validation: X/50 tag + rule pills inline next to leader
- [x] Cost curve height increased (120px bars)

**8. Image Infrastructure**
- [x] `pipeline/migrate-images.ts` — migrates all card/variant images to Cloudflare R2
- [x] `workers/images/` — Cloudflare Worker proxying R2 with correct CORP headers
- [x] All 3,984 production images migrated to `https://optcg-images.corymcunanan.workers.dev`
- [x] Images confirmed loading in production

---

## New Dependencies

```
@radix-ui/react-dialog
@radix-ui/react-tooltip
@radix-ui/react-tabs
@radix-ui/react-select
@radix-ui/react-toast
@radix-ui/react-dropdown-menu
```

**Font:** Barlow Condensed loaded via `next/font/google` in `layout.tsx` — no npm package needed.

`clsx` and `tailwind-merge` are already installed.

---

## Token Spec

### Colors

```css
/* Base surfaces */
--surface-base:    oklch(98% 0.005 75);   /* warm white — page background */
--surface-1:       oklch(96% 0.007 75);   /* slightly warmer — card/panel */
--surface-2:       oklch(93% 0.008 75);   /* elevated panels */
--surface-3:       oklch(88% 0.010 75);   /* tags, badges, dividers */

/* Navy — structure */
--navy-900:        oklch(22% 0.04 245);   /* nav backgrounds, primary text */
--navy-800:        oklch(30% 0.04 245);   /* section headers */
--navy-700:        oklch(38% 0.03 245);   /* borders, subdued elements */
--navy-200:        oklch(88% 0.02 245);   /* light navy tint on surfaces */

/* Gold — secondary accent (treasure/premium moments) */
--gold-500:        oklch(72% 0.14 75);    /* secondary accent */
--gold-400:        oklch(78% 0.12 75);    /* hover */
--gold-100:        oklch(95% 0.04 75);    /* soft gold background */

/* Red — energy */
--red-600:         oklch(55% 0.20 25);    /* primary red, emphasis */
--red-500:         oklch(62% 0.18 25);    /* hover */
--red-100:         oklch(96% 0.04 25);    /* soft red background */

/* Text */
--text-primary:    oklch(18% 0.02 245);   /* navy-tinted, not pure black */
--text-secondary:  oklch(45% 0.01 245);
--text-tertiary:   oklch(62% 0.01 245);
--text-disabled:   oklch(75% 0.01 245);
--text-inverse:    oklch(98% 0.005 75);   /* on dark backgrounds */

/* Borders */
--border:          oklch(86% 0.01 245);
--border-strong:   oklch(76% 0.01 245);
--border-focus:    var(--navy-900);

/* Semantic */
--success:         oklch(52% 0.16 155);
--success-soft:    oklch(96% 0.04 155);
--warning:         oklch(68% 0.14 80);
--warning-soft:    oklch(96% 0.05 80);
--error:           oklch(55% 0.20 25);
--error-soft:      oklch(96% 0.04 25);

/* Overlays */
--overlay:         oklch(10% 0.02 245 / 0.65);
--overlay-light:   oklch(10% 0.02 245 / 0.35);

/* TCG card colors (functional) */
--card-red:        oklch(55% 0.20 25);
--card-blue:       oklch(50% 0.18 250);
--card-green:      oklch(52% 0.18 150);
--card-purple:     oklch(48% 0.18 300);
--card-black:      oklch(28% 0.01 245);
--card-yellow:     oklch(78% 0.18 90);
```

### Spacing

Only these values. Use `gap-`, `p-`, `m-` with Tailwind's default 4px unit scale:

| Token | px | Tailwind |
|-------|----|----------|
| 1     | 4  | `p-1`    |
| 2     | 8  | `p-2`    |
| 3     | 12 | `p-3`    |
| 4     | 16 | `p-4`    |
| 5     | 20 | `p-5`    |
| 6     | 24 | `p-6`    |
| 8     | 32 | `p-8`    |
| 10    | 40 | `p-10`   |
| 12    | 48 | `p-12`   |
| 16    | 64 | `p-16`   |

No `p-2.5`, `px-3 py-1.5`, `p-3.5`, `pt-0.5`, etc. If the value isn't in this table, it's wrong.

### Border Radius

| Name  | Value  | When to use |
|-------|--------|-------------|
| `sm`  | 4px    | Badges, inline tags, small inputs |
| `md`  | 8px    | Buttons, input fields, small cards |
| `lg`  | 12px   | Panels, large cards, modals |
| `full`| 9999px | Pills, avatars, circular elements |

Never use `rounded` (4px, same as sm), `rounded-xl` (12px, same as lg by our definition), `rounded-2xl`, `rounded-3xl`.

### Typography

| Token      | Size | Weight  | Usage |
|------------|------|---------|-------|
| `label-xs` | 11px | medium  | NOT USED — use `xs` |
| `xs`       | 12px | medium  | Labels, captions, badges |
| `sm`       | 14px | regular | Secondary body, form labels |
| `base`     | 16px | regular | Primary body text |
| `lg`       | 18px | medium  | Subheadings, emphasis |
| `xl`       | 20px | semibold| Section titles |
| `2xl`      | 24px | semibold| Page headings |
| `3xl`      | 30px | bold    | Display / hero |

No `text-[9px]`, `text-[10px]`, `text-[11px]`. Minimum rendered text size is 12px (`text-xs`).

---

## Acceptance Criteria

- [x] Zero hardcoded oklch/hex color values in any component file
- [x] Zero `style={{}}` inline objects for design token properties (colors, borders, backgrounds)
- [x] Zero custom `text-[Xpx]` font sizes
- [x] Zero spacing values off the defined scale
- [x] All modals built on `@radix-ui/react-dialog`
- [x] `src/components/ui/` contains: Button, Badge, Input, Dialog, Tooltip, Tabs, Toast
- [x] globals.css implements the full revised token system
- [x] CLAUDE.md reflects the updated design direction
- [x] Deck builder still fully functional after cleanup

## Known Gaps (deferred to future sessions)

- **Leader art variant not persisted to DB** — `leaderSelectedArtUrl` is local state only. When a deck is saved, the selected art URL for the leader is not stored. Follow-up: add `leaderArtUrl` field to `Deck` model and save/load it.
- **Admin area** — still has some removed-token usage. Not yet normalized (out of scope for M2.5, deferred to M2 or dedicated admin pass).
- **Mobile layout** — deck builder not yet responsive. Deferred to M2 responsive pass.

---

## Status: ✅ COMPLETE — 2026-03-18

_Last updated: 2026-03-18_
