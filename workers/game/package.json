{
  "name": "optcg-game",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "wrangler dev --port 8787",
    "deploy": "wrangler deploy",
    "type-check": "tsc --noEmit",
    "schema:check": "node src/engine/schemas/lint-schemas.sh && node --import tsx src/engine/action-inventory-cli.ts && vitest run src/__tests__/opt-471-authored-schema-gate.test.ts",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:coverage": "vitest run --coverage"
  },
  "dependencies": {
    "zod": "^4.3.6"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20241205.0",
    "@types/node": "^20",
    "@vitest/coverage-v8": "4.1.4",
    "typescript": "^5",
    "vitest": "4.1.4",
    "wrangler": "^3"
  }
}
